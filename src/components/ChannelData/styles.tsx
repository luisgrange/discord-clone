import styled from 'styled-components';

export const Container = styled.div`
    grid-area: CD;
    display: block;

    background-color: var(--primary);
    
    position: relative;
    max-height: 100vh;
    overflow: hidden;
`;
